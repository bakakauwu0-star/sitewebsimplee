---
deployment:
  tasks:
    - export DEPLOYPATH=/home/simplees/public_html
    - /bin/mkdir -p $DEPLOYPATH
    - shopt -s dotglob
    - /bin/rm -rf $DEPLOYPATH/*
    - /bin/cp -af * $DEPLOYPATH
    - /bin/rm -rf $DEPLOYPATH/.git $DEPLOYPATH/.github $DEPLOYPATH/.cpanel.yml
